{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "bot",
            "database": "${workspaceFolder:home-work}/bot.db"
        }
    ]
}